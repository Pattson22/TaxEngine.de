# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12   # required for OAuth2PasswordRequestForm (login endpoint)

# --- Data layer ---
sqlalchemy==2.0.35
alembic==1.13.2
psycopg[binary]==3.2.1

# --- Validation / settings ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Auth / security ---
argon2-cffi==23.1.0        # password hashing
python-jose[cryptography]==3.3.0   # JWT session tokens

# --- Payments ---
stripe==10.10.0

# --- Testing ---
pytest==8.3.3
pytest-cov==5.0.0
