# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12   # required for OAuth2PasswordRequestForm (login endpoint)

# --- Data layer ---
sqlalchemy==2.0.35
alembic==1.13.2
psycopg[binary]==3.2.1

# --- Validation / settings ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Auth / security ---
argon2-cffi==23.1.0        # password hashing
python-jose[cryptography]==3.3.0   # JWT session tokens

# --- Payments ---
stripe==10.10.0

# --- Document upload / extraction ---
boto3==1.35.36             # S3-compatible object storage client
anthropic==1.2.0           # Claude document/vision understanding for extraction
python-docx==1.1.2         # text extraction from .docx (Claude's document blocks accept PDF/images, not docx)

# --- PDF generation ---
reportlab==4.2.5           # komprimierte Steuererklärung cover sheet (see app/eric/cover_sheet.py)

# --- Testing ---
pytest==8.3.3
pytest-cov==5.0.0
